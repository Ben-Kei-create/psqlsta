name: SQLCafe
options:
  deploymentTarget:
    iOS: "16.0"
  bundleIdPrefix: com.sqlcafe
  xcodeVersion: "16.0"

targets:
  SQLCafe:
    type: application
    platform: iOS
    sources:
      - path: SQLCafe
    info:
      path: SQLCafe/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.sqlcafe.app
        SWIFT_VERSION: 5.9
        TARGETED_DEVICE_FAMILY: 1,2
